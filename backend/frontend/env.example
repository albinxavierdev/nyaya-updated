# Copy to .env.local and fill in. Do not commit .env.local.

# Google OAuth – Web client ID (same as backend GOOGLE_CLIENT_ID)
# Never put GOOGLE_CLIENT_SECRET here; frontend must not have the secret.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=633662516617-7r9gvfr3kbk093jr1ujoilkmfb0jempc.apps.googleusercontent.com

# Backend URL (defaults to http://localhost:8000 if unset)
NEXT_PUBLIC_SERVER_URL=http://localhost:8000
